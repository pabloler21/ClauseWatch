[project]
name = "clausewatch"
version = "0.1.0"
description = "Analisis multi-agente de cambios entre un contrato y su enmienda, a partir de imagenes escaneadas."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain[openai]>=1.3.18",
    "langchain-openai>=1.6.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "langfuse>=4.15.1",
]
